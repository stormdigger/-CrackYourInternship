
#include <bits/stdc++.h>
using namespace std;


// } Driver Code Ends
//User function Template for C++

class Solution {
  public:
    void permute(vector<int> &arr ,int n,set<vector<int>>&ans,vector<int>&res,vector<int>&vis)
    {
        if(res.size()==n)
        {
            ans.insert(res);
            return;
        }
        for(int i=0;i<n;i++)
        {
            if(!vis[i])
            {
                res.push_back(arr[i]);
                vis[i]=1;
                permute(arr,n,ans,res,vis);
                vis[i]=0;
                res.pop_back();
            }
        }
        
    }
    vector<vector<int>> uniquePerms(vector<int> &arr ,int n) {
        // code here
        set<vector<int>>ans;
        vector<int>res;
        vector<int>vis(n,0);
        permute(arr,n,ans,res,vis);
        vector<vector<int>>fina(ans.begin(),ans.end());
        return fina;
    }
};

//{ Driver Code Starts.

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        
        cin>>n;
        vector<int> arr(n);
        
        for(int i=0 ; i<n ; i++)
            cin>>arr[i];

        Solution ob;
        vector<vector<int>> res = ob.uniquePerms(arr,n);
        for(int i=0; i<res.size(); i++)
        {
            for(int j=0; j<n; j++)
            {
                cout<<res[i][j]<<" ";
            }
            cout<<"\n";
        }
    }
    return 0;
}
